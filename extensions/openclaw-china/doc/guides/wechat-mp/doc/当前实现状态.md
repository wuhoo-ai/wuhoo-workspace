# 微信公众号插件当前实现状态

## 1. 说明

本文用于总结当前仓库中的 `wechat-mp` 插件已经实现了哪些能力、哪些能力只是做了边界预留、哪些能力仍未实现。

这里描述的是“当前代码实际状态”，不是最初计划中的理想范围。

当前结论基于以下内容：

- `doc/guides/wechat-mp/doc/开发计划.md`
- `.workflow/archives/WFS-wechat-mp-plan/IMPL_PLAN.md`
- `extensions/wechat-mp/`
- `packages/channels/`
- `packages/shared/src/cli/`

## 2. 总体结论

当前 `wechat-mp` 已经完成首版最小消息闭环：

1. `wechat-mp` 独立插件已存在，并可被 OpenClaw / 聚合包识别
2. 公众号回调 `GET / POST` 已接入
3. `plain / safe / compat` 三种消息模式具备基础处理边界
4. 文本消息与部分基础事件可以进入 OpenClaw 路由主链路
5. 回复可通过 `passive` 被动回包或 `active` 主动发送文本
6. `activeDeliveryMode` 已支持 `split / merged`
7. setup / aggregate / install hint / release surface 已接线

也就是说：

- 基础文本对话链路：已具备
- webhook 验签与安全模式基础链路：已具备
- OpenClaw routing / session / reply 接入：已具备
- 主动发送文本骨架：已具备
- 仓库集成与发布接线：已具备

但当前仍然只是一个“首版文本渠道”，还不是完整的“公众号平台接入”。

当前仍明显缺少：

- OAuth / JS-SDK / 二维码 / 用户信息等公众号平台能力
- 多账号正式化联调与运维能力
- 更完整的状态面板、错误分类与诊断能力

### 已补充实现的能力（2026-03-21 ~ 2026-03-22）

- ✅ 图片、语音、视频、短视频、位置、链接等完整媒体消息入站
- ✅ 语音 ASR（腾讯云 Flash ASR）自动转文字
- ✅ 模板消息发送
- ✅ 图片、语音、视频主动发送（媒体上传 + 发送）
- ✅ 指数退避重试机制
- ✅ 48 小时交互窗口检测
- ✅ `welcomeText` 关注欢迎语发送
- ✅ 5 秒被动回复超时保护（4.5s 超时检测，后台继续处理并降级为 active 发送）
- ✅ 超长文本分片（active 模式自动分片，passive 模式截断并提示）

另外有一个重要现状需要单独说明：

1. 开发计划里提到的”XML / JSON 消息解析”，当前代码实际上是**XML 主路径**，未看到完整 JSON 入站支持

### 已补充实现的能力（2026-03-22）

- ✅ `welcomeText` 关注欢迎语发送（subscribe 事件触发，通过主动发送 API 发送）

## 3. 已实现能力

### 3.1 插件与仓库接入

已实现：

- 独立插件包：`extensions/wechat-mp/`
- 渠道 ID：`wechat-mp`
- `openclaw.plugin.json` 已声明渠道元信息与配置 schema
- `index.ts` 已注册 `registerChannel({ plugin })`
- 已注册 HTTP 路由 / HTTP handler，用于接管公众号 webhook
- 已接入聚合包：`packages/channels`
- 聚合包 `openclaw.plugin.json` 已把 `wechat-mp` 纳入 channels 列表
- 根 README 已加入微信公众号入口与使用说明
- 发布脚本 `scripts/release-all.mjs` 已把 `wechat-mp` 纳入统一发布流程

结论：

- `wechat-mp` 已不是孤立实验目录，而是已接入仓库主流程的正式渠道插件

### 3.2 配置模型与 setup 接线

当前配置 schema 已支持：

- `enabled`
- `name`
- `defaultAccount`
- `appId`
- `appSecret`
- `encodingAESKey`
- `token`
- `webhookPath`
- `messageMode`
- `replyMode`
- `activeDeliveryMode`
- `welcomeText`
- `dmPolicy`
- `allowFrom`
- `accounts`

当前运行时实际已消费的关键字段：

- `appId`
- `appSecret`
- `encodingAESKey`
- `token`
- `webhookPath`
- `messageMode`
- `replyMode`
- `activeDeliveryMode`
- `dmPolicy`
- `allowFrom`
- `accounts`
- `defaultAccount`

当前 `openclaw china setup` 已支持录入：

- `webhookPath`
- `appId`
- `appSecret`
- `token`
- `messageMode`
- `encodingAESKey`
- `replyMode`
- `activeDeliveryMode`（仅当 replyMode=active 时询问）
- `renderMarkdown`
- `welcomeText`

但当前 setup **还没有**交互式录入：

- `dmPolicy`
- `allowFrom`
- `defaultAccount`
- `accounts`

说明：

- 配置模型已经是 multi-account ready
- 交互式 setup 仍以 default account 为主

### 3.3 鉴权、token、crypto 与 webhook ingress

已实现：

- `appid + appSecret -> access_token`
- `access_token` 缓存
- 失效后清缓存并重取
- 提前刷新定时器
- `GET` 回调校验
- 明文 `POST` 验签
- 安全模式 `POST` 的 `msg_signature` 验签
- AES 解密
- 解密后的 `appid` 校验
- 被动回复加密回包
- 轻量 XML 解析与 XML 构造

当前 webhook 行为：

- `GET`：基于 `signature` 校验
- `GET + aes`：解密 `echostr` 后返回明文结果
- `POST plain`：按明文请求处理
- `POST safe/compat`：识别 `Encrypt` 字段后按密文链路验签、解密、再进入标准化流程

说明：

- 当前实现是“XML 主路径”
- 虽然开发计划中写了“XML / JSON 消息体解析与标准化”，但当前代码没有看到完整 JSON 入站解析实现
- `compat` 当前更接近“按收到的请求体形态决定 plain / encrypted 分支”，而不是单独做一套复杂兼容层

### 3.4 入站标准化、去重与 OpenClaw 主链路对接

已实现：

- `openid -> target` 映射
- `appId + openid -> sessionKey` 映射
- target 格式支持：
  - `user:<openid>`
  - `user:<openid>@<accountId>`
  - `wechat-mp:user:<openid>`
  - `wechat-mp:user:<openid>@<accountId>`
- session key 格式：`dm:<appId>:<openid>`
- 接入 `resolveAgentRoute`
- 接入 `dispatchReplyWithBufferedBlockDispatcher`
- 记录 inbound session 和最近 route
- 入站上下文当前统一写入 `CommandAuthorized=true`，slash command 可直接透传
- `dmPolicy`
- `allowFrom`
- `msgid` / dedupeKey 去重
- 基础账号状态更新

当前真正完成标准化并可进入后续处理的入站类型：

1. 文本消息 `text`
2. 图片消息 `image` ✅ (2026-03-21)
3. 语音消息 `voice` ✅ (2026-03-21) — 支持 ASR 自动转文字
4. 视频消息 `video` ✅ (2026-03-21)
5. 短视频消息 `shortvideo` ✅ (2026-03-21)
6. 位置消息 `location` ✅ (2026-03-21)
7. 链接消息 `link` ✅ (2026-03-21)
8. 基础事件：
   - `subscribe`
   - `unsubscribe`
   - `scan`
   - `click`
   - `view`

但这几类事件不是全部都进入 Agent：

- 会进入 Agent 主链路：
  - `text`
  - `image`
  - `voice`（含 ASR 转写结果）
  - `video`
  - `shortvideo`
  - `location`
  - `link`
  - `subscribe`
  - `scan`
  - `click`
- 只被识别 / 记录，不进入 Agent 主链路：
  - `unsubscribe`
  - `view`

说明：

- `types.ts` 已声明所有消息类型结构
- `inbound.ts` 现已处理 `text`、`image`、`voice`、`video`、`shortvideo`、`location`、`link` 及所有基础事件
- 不支持的消息类型当前会返回 `success`，并更新部分状态，但不会进入 Agent 主链路
- 去重状态当前持久化在本地：`~/.openclaw/wechat-mp/data/state.json`
- 去重 TTL 当前为 10 分钟

### 3.5 回复与出站

已实现：

- 被动文本回复
- 安全模式被动回包加密
- 主动文本发送辅助函数
- 公众号客服消息 API 发送文本
- `replyMode=passive`
- `replyMode=active`
- `activeDeliveryMode=merged`
- `activeDeliveryMode=split`
- host outbound surface：`wechatMpOutbound.sendText`
- ✅ 模板消息发送（2026-03-21）
- ✅ 媒体上传（图片/语音/视频）
- ✅ 图片主动发送（2026-03-21）
- ✅ 语音主动发送（2026-03-21）
- ✅ 视频主动发送（2026-03-21）
- ✅ 发送能力检测（48小时交互窗口）
- ✅ 指数退避重试机制（2026-03-21）

当前行为边界如下：

#### passive

- 等待 reply pipeline 产出最终文本
- 如果拿到最终文本，则直接回 XML
- 在 `plain` 模式下回明文 XML
- 在 `safe / compat` 且具备密钥时回加密 XML
- `activeDeliveryMode` 在 `passive` 模式下不生效

#### active + merged

- 等 reply pipeline 完成后
- 把多个文本块合并为一条消息主动发送

#### active + split

- 每个非空 chunk / 文本块单独主动发送一条消息

当前出站能力的实际边界：

- 文本发送：✅ 已完整实现
- 模板消息：✅ 已实现
- 图片发送：✅ 已实现（自动上传 + 发送）
- 语音发送：✅ 已实现（自动上传 + 发送）
- 视频发送：✅ 已实现（自动上传 + 发送）
- 重试机制：✅ 已实现（指数退避）
- 发送能力检测：✅ 已实现（48小时交互窗口检测）
- 定时通知能力：❌ 仍需开发

说明：

- `api.ts` 提供完整的媒体上传和消息发送 API
- `send.ts` 封装了文本、模板消息、媒体消息的发送逻辑
- `outbound.ts` 对外暴露统一的出站接口

### 3.6 API 预留能力

当前代码里已经出现但尚未形成正式插件能力的 API helper：

- 临时媒体上传
- 自定义菜单创建
- 自定义菜单删除

这些能力当前状态应视为：

- 底层 API helper 已存在
- 但没有接入正式模块边界
- 没有接入 setup / onboarding / 文档主路径
- 没有围绕这些能力形成主链路测试

因此它们更适合归类为“后续能力预留”，不应视为当前已完成的平台功能。

### 3.7 运维、状态与 onboarding

已实现：

- 基础 onboarding adapter
- 基础 probe 能力
- gateway start / stop 时更新账号状态
- 状态记录项包括：
  - `configured`
  - `running`
  - `webhookPath`
  - `lastStartAt`
  - `lastStopAt`
  - `lastInboundAt`
  - `lastOutboundAt`
  - `lastIntentfulAt`
  - `lastError`
  - `lastMessageId`
  - `lastEvent`
  - `lastFromUserName`
- install hint 已支持 `wechat-mp`

当前 probe 的能力边界：

- 可判断配置是否完整
- 若具备主动发送条件，会尝试获取 `access_token`
- 会返回 `configured / canSendActive / webhookPath / authOk / lastInboundAt`

当前 onboarding 的能力边界：

- 可显示“是否已配置”
- 可显示 webhookPath、replyMode、最近错误
- 会给出公众号接入的基础提示

但当前仍缺：

- 更完整的运行状态面板
- webhook 注册状态详情
- 结构化监控指标
- 更细粒度错误分类
- 后台管理命令

### 3.8 测试

当前已覆盖的测试重点包括：

- crypto：
  - `signature` 计算与校验
  - `msg_signature` 计算与校验
  - safe-mode 加解密
  - XML 解析
- token：
  - token 获取
  - token 缓存
  - cache clear 后刷新
  - invalid token errcode 判断
- dispatch：
  - 文本主链路 dispatch
  - 非意图事件跳过
  - 意图事件进入主链路
  - chunk merge
  - split chunk 转发
- webhook：
  - `GET plain` 校验
  - `GET safe` 校验
  - `POST` 加密文本消息处理
  - 明文签名错误拒绝
  - 重复消息去重
  - passive reply XML 返回
  - slash command 场景下 `CommandAuthorized=true`
  - passive 模式忽略 `activeDeliveryMode=split`
  - active merged / active split 两种主动发送行为
- china setup：
  - `wechat-mp` 配置写入

说明：

- 当前测试覆盖的是“单元 + 局部集成”层面
- 尚未看到真实公众号后台联调 / E2E 自动化测试
- 尚未看到多账号专项测试
- 尚未看到媒体、菜单、二维码、OAuth、JS-SDK 等后续能力测试

## 4. 部分实现但还不能算完整能力

### 4.1 多账号

当前状态：代码结构已预留，但还不能算正式完成

已经有的部分：

- schema 支持 `accounts` / `defaultAccount`
- route path 收集已考虑多账号路径
- target 格式支持 `@accountId`
- account config merge 已实现
- gateway 启停逻辑支持按 account 启动

仍然缺的部分：

- setup 没有完整多账号交互流程
- 缺少多账号实施文档
- 缺少多账号专项测试
- 缺少多账号状态展示与冲突治理说明
- 当前默认使用路径仍是单账号优先

结论：

- 代码结构上是 multi-account ready
- 但当前不应把它当成“稳定完成的多账号能力”

### 4.2 主动发送能力

当前状态：已完成文本和媒体主动发送，形成较完整的主动发送体系

已经有的部分：

- 文本主动发送
- `split / merged` 两种主动发送形态
- host outbound 文本接口
- ✅ 模板消息发送（2026-03-21）
- ✅ 图片主动发送（2026-03-21）
- ✅ 语音主动发送（2026-03-21）
- ✅ 视频主动发送（2026-03-21）
- ✅ 指数退避重试机制（2026-03-21）
- ✅ 48小时交互窗口检测（2026-03-21）
- ✅ 媒体自动上传并发送

仍然缺的部分：

- 会话外通知能力
- 定时任务 / scheduler
- 更完整的发送失败补偿

结论：

- 当前 `active` 已具备较完整的消息发送能力
- 支持文本、模板消息、图片、语音、视频等主动发送
- 重试机制和时间窗口检测已实现

## 5. 当前未实现的能力

### 5.1 公众号平台能力

当前未实现：

- OAuth 网页授权
- JS-SDK 签名服务
- 带参数二维码
- 用户基础信息拉取
- 模板消息发送与投递结果处理
- 完整自定义菜单能力
- H5 / 小程序相关平台扩展

说明：

- 开发计划把这些能力列为 P1 / P2
- 当前代码目录里也没有形成 `oauth.ts`、`js-sdk.ts`、`qrcode.ts` 等正式模块

### 5.2 媒体与消息类型

#### 已实现（2026-03-21）

入站：
- ✅ 图片消息入站处理
- ✅ 语音消息入站处理（含 ASR 自动转文字）
- ✅ 视频消息入站处理
- ✅ 短视频消息入站处理
- ✅ 位置消息入站处理
- ✅ 链接消息入站处理
- ✅ 入站媒体下载（语音文件用于 ASR）

出站：
- ✅ 图片发送
- ✅ 语音发送
- ✅ 视频发送
- ✅ 模板消息发送

#### 仍未实现

- 图文 / 卡片类出站
- 入站媒体落盘与清理（持久化存储）
- 图文消息（news）出站

### 5.3 更完整的运维与诊断

当前未实现：

- 结构化监控指标
- 更细粒度错误码分类
- 后台管理命令
- 更丰富的 probe 输出
- 定时健康检查任务
- 主动告警 / 报警

### 5.4 开发计划中仅部分完成的点

以下项目已部分完成，但不能算完全按计划落地：

1. **XML / JSON 消息体解析与标准化**
   - 当前实际以 XML 为主
   - 未见完整 JSON 入站支持

2. **更完整的状态与探测**
   - 已有基础 probe / onboarding / state
   - 但距离”完整运行状态与探测”还有明显差距

## 6. 与开发计划对照后的当前判断

### 6.1 可以视为已经完成的 P0 主项

可以基本视为完成：

- 独立插件骨架
- `channels.wechat-mp` 配置模型
- `access_token` 获取、缓存、刷新
- 回调 GET 验证
- 回调 POST 验签
- 明文模式消息接入
- 安全模式 / 兼容模式的基础处理边界
- 文本消息入站
- 基础事件识别
- OpenClaw routing / session / reply 主链路接入
- `msgid` 去重
- 基础状态记录
- 接入 `packages/channels`
- setup / install hint / README / release surface 接线

### 6.2 只能算”部分完成”的 P0 项

部分完成：

- **XML / JSON 解析与标准化**：当前主要完成 XML
- **基础事件入站**：已识别，但不是所有事件都进入 Agent
- **运维与探测**：已有基础版本，但还不够完整

### 6.3 仍属于 P1 / P2 的未完成项

仍未完成：

- OAuth
- JS-SDK
- 菜单管理正式能力
- 二维码能力
- 用户资料增强
- 模板消息
- 图片 / 语音等媒体消息主链路
- 多账号正式化
- 更完整的主动发送能力
- 更完整的运行状态与诊断

## 7. 一句话结论

当前 `wechat-mp` 已经是一个功能较完整的”微信公众号渠道实现”：它完成了 webhook 接入、全量消息类型入站（text/image/voice/video/shortvideo/location/link）、语音 ASR 自动转文字、被动回复主路径、主动发送体系（文本/模板/图片/语音/视频）、重试机制、时间窗口检测和关注欢迎语。

但仍缺少 OAuth / JS-SDK / 二维码 / 用户信息等公众号平台能力、多账号正式化运维能力，以及更完整的运维诊断能力，仍然需要后续实现。

## 8. 2026-03-21 更新日志

### 新增功能

1. **全量消息类型入站**
   - 图片消息（`image`）：返回图片 URL 和 MediaId
   - 语音消息（`voice`）：支持 ASR 自动转文字，返回格式和 MediaId
   - 视频消息（`video`）：返回视频 MediaId 和缩略图 MediaId
   - 短视频消息（`shortvideo`）：返回短视频 MediaId
   - 位置消息（`location`）：返回经纬度和地址
   - 链接消息（`link`）：返回标题、描述和 URL

2. **语音转文字（ASR）**
   - 集成腾讯云 Flash ASR 录音文件识别极速版
   - 支持配置 ASR 参数：appId、secretId、secretKey、engineType、timeoutMs
   - 优先使用微信内置语音识别结果（需公众号后台开启）
   - ASR 失败时提供友好错误提示

3. **主动发送增强**
   - 模板消息发送（`sendTemplate`）
   - 图片消息发送（`sendMedia` + 自动上传）
   - 语音消息发送（`sendMedia` + 自动上传）
   - 视频消息发送（`sendMedia` + 自动上传）

4. **发送能力检测**
   - 48 小时交互窗口检测（`checkCapability`）
   - 时间窗口配置（`checkTimeWindow`）
   - 用户交互记录（`recordInteraction`）

5. **重试机制**
   - 指数退避重试（`retryConfig`）
   - 可配置参数：maxRetries、initialDelay、maxDelay、backoffMultiplier

### 文件变更

- `extensions/wechat-mp/src/types.ts`：新增所有消息类型和 ASR 配置
- `extensions/wechat-mp/src/inbound.ts`：新增 image/voice/video/shortvideo/location/link 消息处理
- `extensions/wechat-mp/src/dispatch.ts`：新增语音 ASR 异步处理
- `extensions/wechat-mp/src/api.ts`：新增 `downloadWechatMpMedia` 媒体下载
- `extensions/wechat-mp/src/send.ts`：新增模板消息和媒体发送
- `extensions/wechat-mp/src/outbound.ts`：对外暴露完整出站接口
- `extensions/wechat-mp/src/config.ts`：新增 `resolveWechatMpASRCredentials`

## 9. 2026-03-22 更新日志

### 新增功能

1. **关注欢迎语发送**
   - `welcomeText` 配置项现在会在用户关注（subscribe 事件）时自动发送
   - 使用主动发送 API，需要配置 `appSecret` 才能生效
   - 欢迎语在 Agent 主链路处理之前发送

### 文件变更

- `extensions/wechat-mp/src/webhook.ts`：在 `handoffInboundCandidate` 中添加欢迎语发送逻辑

### 2026-03-22 新增功能（续）

1. **5 秒被动回复超时保护**
   - 使用 4.5 秒超时检测（预留 500ms 缓冲）
   - 超时后立即返回 "success" 给微信，避免重试
   - 后台继续处理，处理完成后降级为 active 发送（需配置 appSecret）
   - 记录超时状态到 `lastError`

2. **超长文本分片与截断**
   - active 模式：超过 2048 字节自动分片发送（原有功能）
   - passive 模式：超过 2048 字节自动截断并添加"…[消息过长已截断]"提示
   - 新增 `truncateTextByByteLimit` 函数用于截断处理

### 文件变更（续）

- `extensions/wechat-mp/src/webhook.ts`：添加 `WECHAT_PASSIVE_TIMEOUT_MS` 常量和超时竞争逻辑
- `extensions/wechat-mp/src/send.ts`：passive 回复添加超长文本截断
- `extensions/wechat-mp/src/text.ts`：新增 `truncateTextByByteLimit` 函数